server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Suhelbashakalaigiri/cartigo-config
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers
