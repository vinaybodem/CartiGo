server:
  port: 9010

spring:
  application:
    name: reporting-analytics-service
  datasource:
    url: jdbc:mysql://localhost:3306/cartigo_analytics?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
