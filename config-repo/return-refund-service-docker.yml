server:
  port: 9008

spring:
  application:
    name: return-refund-service
  datasource:
    url: jdbc:mysql://mysql:3306/cartigo_return?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

