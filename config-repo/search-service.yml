server:
  port: 9013

spring:
  application:
    name: search-service
  datasource:
    url: jdbc:mysql://localhost:3306/cartigo_search?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  elasticsearch:
    uris: http://localhost:9200
