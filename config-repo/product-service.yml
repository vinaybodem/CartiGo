server:
  port: 9003

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/cartigo_product?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
