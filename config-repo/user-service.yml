server:
  port: 9002

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/cartigo_user?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
