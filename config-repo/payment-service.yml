server:
  port: 9006

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cartigo_payment?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

#razorpay:
#  key-id: ${RAZORPAY_KEY_ID}
#  key-secret: ${RAZORPAY_KEY_SECRET}