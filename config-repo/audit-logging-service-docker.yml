server:
  port: 9012

spring:
  application:
    name: audit-logging-service
  datasource:
    url: jdbc:mysql://mysql:3306/cartigo_audit?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

